# 夜间推进状态报告

**2026-09-13 05:40 AEST** · 距 16:00 功能冻结 **10h20m** · 距周一 12:00 提交 **30h20m**

一句话：**代码骨架和确定性逻辑全部完成并验证过；所有需要 API key 的部分写完了但一次都没真跑过。**

---

## ✅ 验证过的（可以信）

| | 证据 |
| --- | --- |
| 确定性管线 ⑥⑦⑧ + 引文校验 ③b | **26 个离线测试全过** |
| 12 个场景，六个根因全覆盖 | **24/24 正确**（升级判定 + 阻断点） |
| Web 应用，12 个案例页 + JSON API + 手机视口 | 逐个 200，手机截图确认 |
| 评测框架，三个失败率分开报 | 跑通 |
| **服务器部署** | 容器在跑，`/health` 返回 12 个场景，**内存 48MB / 1.57GB** |
| 转公开前密钥扫描 | 工作区 + **全部提交历史**都干净 |

## 🟡 写完但从未真实调用过（**不能信，要先测**）

**没有任何 API key**，所以这些只有离线测试覆盖：

| 模块 | 覆盖到的 | 没覆盖的 |
| --- | --- | --- |
| `pipeline/extract.py` LLM 抽取 | JSON 解析、字段校验、越界丢弃、降级 | **真实模型输出长什么样** |
| `pipeline/resolution.py` 层 ⑤ | 同上 | 同上 |
| `audio/tts.py` 语料生成 | 计价、缓存、`--dry-run` | **真实 API 调用** |
| `audio/stt.py` Scribe 转录 | 词流折叠成对话、缓存 | **真实 API 调用** |

> **第一次真调用请当成测试，不要当成 demo。** 一定会有问题。

## ⚠️ 评测数字目前不算数

升级 100%、阻断点 12/12 —— **跑的是 `LabelledExtractor`，它直接读答案**。
报告底部原样印着这句话，我没有藏。**换成真 LLM 抽取之后才是真实测量。**

---

## 今早写代码逮到的五个真 bug

这是"先让系统能端到端跑"的实际价值：

1. **demo-004 判 39 分（该升级）** —— 回电准时打了、承诺兑现了，只盯承诺的系统看不见 → 加 `no_owner`
2. **demo-003 阻断点判错** —— 第 1 通答复其实是对的，失败在第 2 通矛盾答复 → 矛盾优先于"最早未解决诉求"
3. **`repeat_contact` 对任何联系两次的客户都触发** —— 包括第二通正是兑现承诺的回电 → 要求确实有东西没解决
4. **demo-008 判 27 分（该升级）** —— 客户手机被误停，但评分不知道"客户正没有服务可用" → 加 `service_loss`
5. **demo-009 是我的标注错了** —— 系统判第 1 通是对的，我标的第 2 通不对 → **改标注不改代码**，并在场景文件里写明原因

第 3 个特别说明**反例为什么必须写**：没有它们，一个逢人就报警的系统评测也是满分。

---

## 🔴 阻塞项，按优先级

| # | 卡什么 | 谁能解 |
| --- | --- | --- |
| 1 | **没有任何 API key** —— 音频和真实评测都动不了 | 全员领 ElevenLabs；Anthropic key 需要有人提供 |
| 2 | **阿里云安全组 80/443 + DuckDNS 子域名** | **只有 @linether** |
| 3 | Demo 视频 —— 分镜草稿有了，**没人起头录** | 全队 |

---

## 交付统计

```
60 次提交 · 19 个代码文件 · 约 2,500 行 · 12 个场景 · 30 份文档
```

**周六 12:00 之前的提交全部是规划文档，没有一行应用代码。历史未经任何改写。**

---

## 醒来后的第一小时建议

1. **领 ElevenLabs key**（全员，5 分钟）—— 阻塞最多东西
2. **开安全组 + DuckDNS**（@linether，5 分钟）→ `SITE_DOMAIN=xxx.duckdns.org ./deploy.sh`
3. **Genicayyy 跑第一次真实抽取** —— 今天最关键的一步
4. 三个人各自 `git pull` 然后跑一遍，确认环境没问题

任务分工写在 `BOARD.md` 最底部和 Issue #3。
时间表和每种失败的兜底方案在 `docs/timeline.md`。

**14:00 锁定最小可交付版本** —— 做完那一步，就不用再担心"交不出来"这件事了。
