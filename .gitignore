# Secrets
.env
.env.local
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
dist/
build/
.next/
*.log
npm-debug.log*
pnpm-debug.log*

# OS / editors
.DS_Store
.vscode/
.idea/

# Data — keep big/private files out of git
data/raw/
*.sqlite
*.db

# venv
.venv/

# Cached Scribe responses — regenerable, and they cost credits.
data/transcripts/

# Generated audio IS committed. It is small (a few MB), it costs credits to
# regenerate, and the deployed site serves it so judges can hear the calls —
# hearing the angry-but-resolved caller next to the calm-but-escalating one
# lands harder than reading either.

# Intermediate video artefacts. The finished assets/demo.mp4 is committed.
build/
